package workshop;

  /*
  * takes data from source (its a URL path for the BB).
  * upload the prof and new votes to the dest path.
  * sign with the privateSig Key
  */
  public void Mixnet(String source, String dest, int privateSig);
